# CH4Homework9

내일배움캠프 9번 과제

****필수 기능****

- **GameModeBase의 판정 로직 구현**
    - 기본적인 멀티플레이 채팅 구현.
    - 정답 숫자 3자리를 생성하는 함수 구현.
        - 게임 시작시 서버에서 1~9사이 중복되지 않는 3자리 숫자 생성.
        - ex 386, 594, …
    - 제시된 숫자가 숫자야구를 위한 숫자인지 판단하는 함수 구현.
        - 3자리인지, 문자가 들어갔는지, 중복되는 숫자가 없는지, …
    - 제시된 숫자와 정답 숫자를 비교해서 S/B/Out 여부를 계산하는 함수 구현.
        - 자리수와 값이 같으면 S, 자리수는 다르지만 값이 존재하면 B, 둘 다 아니면 OUT.
        - 위 기준을 토대로 결과 문자열 반환. (ex. “2S0B”, “OUT”, “0S1B”, …)
    
- **PlayerState 클래스를 활용한 시도 횟수 관리**
    - 각 플레이어들의 최대 시도 횟수 3회와 현재 시도 횟수가 관리.
    - 채팅을 작성할 때 마다 현재 시도 횟수와 최대 시도 횟수가 보이게끔 구현.
        - 현재 시도 횟수와 최대 시도 횟수 관련 Getter 함수 구현.
        - 적절한 클래스에서 해당 Getter 함수 호출.
    - 플레이어가 숫자 야구를 할때마다 현재 시도 횟수가 오르게끔 구현.
    
- **승리, 무승부, 게임 리셋**
    - 게임 결과(승리, 무승부)를 알리기 위한 공지 위젯 생성.
    - 적절한 클래스에서 현재 게임 결과를 판단하는 멤버 함수 구현.
        - 3S를 먼저 달성한 플레이어가 누군지 공지 위젯으로 알림.
        - 모두가 최대 시도 횟수만큼 숫자 야구를 진행했다면 무승부를 공지 위젯으로 알림.
    - 게임 결과가 나오면 게임을 리셋하는 멤버 함수 구현.

****도전 기능****

- **턴 제어 기능**
    - 몇 초가 남았는지 보여주는 위젯 스폰.
    - 적절한 클래스에서 플레이어 턴의 남은 시간을 관리 및 위젯과 바인드.
    - 적절한 클래스에서 각 플레이어별 턴의 남은 시간 업데이트 구현.
    - 남은 시간이 없는 경우, 숫자 야구 플레이 불가 구현.
    
- **턴 내에 숫자 야구 하지 않을시 기회 소진 기능**
    - 턴 내에 숫자 야구를 했는지 안했는지에 대한 속성 추가.
    - 적절한 클래스에서 위 속성에 대한 수정 및 기회 소진 로직 추가.

**도전 기능 구현 내용**

- 타이머를 구현하여 자신의 남은 시간 확인 가능
- 타이머가 종료될 시 자신의 턴 종료
- 화면에 누구의 턴인지 명시
- 자신의 턴이 아닐 시 정답이더라도 확인하지 않음



 
플로우

플레이어 접속 → 
GameMode PlayerState 이름 지정 →
GameState “OOO has joined the game” 브로드캐스트 →
로컬 PlayerController가 UserWidget 생성 →
유저가 채팅 입력시 PlayerController가 RPC호출 →
GameMode가 게임 관련 채팅인지 검사 →
맞을 시 결과 브로드 캐스트 

도전 기능 플로우

게임 시작 채팅(/start)입력 →
현재 방에 있는 플레이어 배열에 저장 →
게임 관련 변수 초기화 →
타이머 시작 →
타이머가 PlayerController에 TimerText를 수정 →
TimerWidget은 TimerText값 반영 →
타이머가 종료될 시 턴 종료


